# Tasks

## 1. 工作根与运行时标识

- [x] 1.1 将根配置、写锁、执行记录和环境变量统一到 WorkHub 命名；运行核心测试验证安装、读取、锁和日志路径。
- [x] 1.2 更新预览、CLI 输出、Trellis 任务文件和 AGENTS 区块；运行任务创建及 `check` 测试验证生成与检查一致。

## 2. 仓库说明与规格

- [x] 2.1 更新 README、当前开发与集成文档、代理约定及 CHANGELOG；检索当前文档确认只描述现行命名。
- [x] 2.2 清理不适用的仓库内归档说明与验证快照，更新主规格并执行全仓文本及路径检查，确认只剩真实账号、署名和依赖校验串中的对应字符。

## 3. 集成验收

- [x] 3.1 运行 `npm run check`、`npm run build`、`npm run check:version` 和 `npm test`，确认全部通过。
- [x] 3.2 使用隔离临时根目录验收 CLI，并检查 `npm pack` 包含必需模板、LICENSE 和可执行入口；记录验证结果。
- [x] 3.3 同步与归档 OpenSpec，校验最终规格；提交完整变更并推送 origin，确认工作树状态。
