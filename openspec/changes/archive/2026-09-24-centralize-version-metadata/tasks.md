# 任务：集中管理版本元数据

## 1. CLI 版本来源

- [x] 1.1 让 `src/cli.ts` 从运行包的 `package.json` 读取版本，并增加 `wk --version` 与清单版本一致的测试；以 `npm run build` 和该测试通过验证。
- [x] 1.2 在隔离目录安装本地打包文件，验证安装后的 `wk --version` 与包内清单一致；保留实际命令与结果，不修改正式全局安装。

## 2. 自动一致性检查

- [x] 2.1 增加只读版本检查，比较 `package.json`、`package-lock.json` 顶层与根包版本、构建后 CLI 输出；用一致及人为制造分歧的测试确认成功、失败与错误信息。
- [x] 2.2 将版本检查接入 npm 脚本和构建后的 CI 步骤，并在开发说明中写明运行命令；验证本机命令通过且普通未打新标签的提交不受阻。

## 3. 文档与发布节点

- [x] 3.1 将 README 的本地打包安装示例改为使用打包结果文件名；调整当前开发计划和 CLI 契约的易过期版本表述，将发布验证数字标为有日期的历史快照；运行示例并检查当前文档没有无日期的固定“最新版本”断言。
- [x] 3.2 编写中文发布核对步骤，说明 `package.json`、Git 标签、npm registry 的不同状态，以及版本更新、目标提交、远端标签冲突和公开安装的检查方法；用现有 0.1.2 历史标签只读演练，确认步骤不创建标签或发包。

## 4. 集成验证

- [x] 4.1 运行 `npm run check`、`npm run build`、版本检查、`npm test`、`npm pack --dry-run` 和 OpenSpec 严格验证，核对包内 LICENSE 与模板、Git 差异及文档链接；记录结果，并确认本次未改版本号、打标签或发布 npm。
